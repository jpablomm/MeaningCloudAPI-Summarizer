const getSummary = require("./getSummary")

describe("Testing the getSummary function from formHandler", () => {
    test("getSummary function should be defined", () => {
      expect(getSummary).toBeDefined();
    });
  });